#include "Authority.h"

Authority::Authority() { email = "-", password = "-"; }
Authority::~Authority() { }
Authority::Authority(const Authority& other) { *this = other; }

void Authority::Authority_Operation()
{

}
